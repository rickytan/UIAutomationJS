/**
 * The UIAKey class allows access to, and control, of key elements within your app’s keyboard.
 * @constructor
 * @extends UIAElement
 */
var UIAKey = function() {

};

UIAKey.prototype = UIAElement.prototype;
