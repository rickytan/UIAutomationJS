/**
 * The UIATextView class allows access to, and control of, text view elements in your app.
 * @constructor
 */
var UIATextView = function () {

};
UIATextView.prototype = UIAElement.prototype;

/**
 * Sets the specified text view to the specified value.
 * @param {String} value - A string containing the text to populate the text view.
 */
UIATextView.prototype.setValue = function (value) {
};