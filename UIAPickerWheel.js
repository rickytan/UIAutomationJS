/**
 * The UIAPickerWheel class allows access to, and control of, wheel elements within a picker.
 * @constructor
 * @extends UIAPicker
 */
var UIAPickerWheel = function () {

};

UIAPickerWheel.prototype = UIAPicker.prototype;

/**
 * Drags the wheel to the first row with the specified value.
 */
UIAPickerWheel.prototype.selectValue = function () {
};
/**
 * Returns an array representing the possible item values to select for the wheel.
 * @returns {Array}
 */
UIAPickerWheel.prototype.values = function () {
    return [];
};